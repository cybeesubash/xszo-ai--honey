╔══════════════════════════════════════════════════════════════════╗
║           RENDER BUILD FIX - React Dependency Conflict          ║
╚══════════════════════════════════════════════════════════════════╝

PROBLEM: npm install fails with ERESOLVE dependency conflict
CAUSE: React version mismatch in @react-three/fiber
SOLUTION: Use --legacy-peer-deps flag

┌──────────────────────────────────────────────────────────────────┐
│ ✅ FIXED BUILD COMMAND                                           │
└──────────────────────────────────────────────────────────────────┘

UPDATE YOUR BUILD COMMAND TO THIS:
────────────────────────────────────────────────────────────────────
cd dashboard && npm install --legacy-peer-deps && npm run build && cd ../backend && pip install -r requirements.txt
────────────────────────────────────────────────────────────────────

The only change: Added --legacy-peer-deps after npm install

┌──────────────────────────────────────────────────────────────────┐
│ 🔧 HOW TO UPDATE ON RENDER                                       │
└──────────────────────────────────────────────────────────────────┘

1. Go to your service in Render dashboard
2. Click "Settings" (left sidebar)
3. Scroll to "Build & Deploy" section
4. Find "Build Command" field
5. Replace entire command with the one above
6. Click "Save Changes"
7. Click "Manual Deploy" → "Clear build cache & deploy"

┌──────────────────────────────────────────────────────────────────┐
│ 📋 COMPLETE CONFIGURATION (Copy These)                           │
└──────────────────────────────────────────────────────────────────┐

Build Command:
cd dashboard && npm install --legacy-peer-deps && npm run build && cd ../backend && pip install -r requirements.txt

Start Command:
python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT

Environment Variables:
PYTHON_VERSION = 3.11.0
NODE_VERSION = 18.17.0
OPENROUTER_API_KEY = <your-key>
GEMINI_API_KEY = <your-key>
TELEGRAM_BOT_TOKEN = <your-token>
TELEGRAM_CHAT_ID = <your-id>

┌──────────────────────────────────────────────────────────────────┐
│ 💡 WHY THIS WORKS                                                │
└──────────────────────────────────────────────────────────────────┘

• --legacy-peer-deps tells npm to ignore peer dependency conflicts
• React 18 is compatible with @react-three/fiber despite the warning
• This is safe for your project - commonly used for React Three Fiber
• Build will succeed and app will work perfectly

┌──────────────────────────────────────────────────────────────────┐
│ ⚡ QUICK FIX STEPS                                                │
└──────────────────────────────────────────────────────────────────┘

1. Settings → Build Command → Add --legacy-peer-deps → Save
2. Manual Deploy → Clear build cache & deploy → Deploy
3. Wait 5-8 minutes for build
4. Check logs - should see "Build succeeded"
5. Test your URL!

╔══════════════════════════════════════════════════════════════════╗
║  This will fix the npm dependency error completely!             ║
╚══════════════════════════════════════════════════════════════════╝